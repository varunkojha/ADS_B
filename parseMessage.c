#include "headers.h"
#include "declarations.h"

Aircraft* parseMessage(Aircraft * message)
{
	#ifdef DEBUG
	printf("%s: BEGIN \n",__func__);
        #endif

	#ifdef DEBUG
	printf("%s: END \n",__func__);
        #endif

	return message;
}
